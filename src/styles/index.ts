import {
  Button,
  SectionDescription,
  SectionSubTitle,
  SectionTitle,
} from "./common";
import { Row, Column, Subtext, Container } from "./style-components";

const Styles = {
  Column,
  Row,
  Subtext,
  Container,
  Button,
  SectionTitle,
  SectionSubTitle,
  SectionDescription,
};

export default Styles;
